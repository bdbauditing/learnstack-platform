# HTML Forms — Submission
# Instructions: Replace every TODO with your observed values or written answers.

form_observation:
  method: TODO              # What HTTP method did the form use?
  action_url: TODO          # What URL did the form submit to?
  content_type: TODO        # What Content-Type header was sent? (check Payload/Headers tab)
  encoded_body_example: TODO  # Copy the raw encoded body string (e.g. custname=alice&custemail=...)

post_redirect_get:
  # The PRG pattern prevents duplicate form submissions on page refresh.
  # Describe each step:
  step_1: TODO  # What does the user do? (e.g. "User submits the form...")
  step_2: TODO  # What does the server respond with?
  step_3: TODO  # What does the browser do next?
  why_important: TODO  # Why does this prevent duplicate submissions?

file_upload:
  enctype_needed: TODO   # What enctype attribute is needed for file uploads?
  why_different: TODO    # Why can't application/x-www-form-urlencoded handle file uploads?

reflection:
  most_confusing_part: TODO  # What was most confusing about form encoding?
