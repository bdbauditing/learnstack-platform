# Starter — Regression Suite Selection

Read the sprint change log and the full test list in `all-tests.yaml`. Select 10 tests for regression in `regression-selection.yaml`.

Think: which tests cover the changed code + their neighbors?
