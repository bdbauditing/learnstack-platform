# Starter — State Transition Matrix

Fill in the next state for each current state + event combination in `transitions.yaml`.

Use the state name (e.g. `submitted`) or `"invalid"` if the transition should not be allowed.
