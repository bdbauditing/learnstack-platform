# Starter — Traceability Matrix

Link requirements, test cases, and defects in `traceability.yaml`.

Rules:
- Every requirement needs at least 1 test case
- Every defect needs at least 1 test case that would catch it
