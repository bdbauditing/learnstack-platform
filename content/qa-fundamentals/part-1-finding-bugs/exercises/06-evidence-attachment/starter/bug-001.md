# [Login] Submit button disabled with valid credentials

**Severity:** High
**Priority:** High

## Environment

- **Browser:** Chrome 130
- **OS:** macOS 14
- **Viewport:** 1440x900
- **URL:** https://learnstack-taskforge-web.onrender.com/login

## Steps to Reproduce

1. Navigate to `https://learnstack-taskforge-web.onrender.com/login`
2. Enter `bob@taskforge.io` in the Email field
3. Enter `Password1!` in the Password field
4. Observe the Submit button state

## Expected Behavior

The Submit button should become enabled after both fields contain valid input.

## Actual Behavior

The Submit button remains visually disabled (grayed out) even with valid credentials in both fields.

## Evidence

**Screenshot:** <!-- e.g. login-submit-disabled-chrome130.png -->

**Console:** <!-- paste the relevant DevTools error line here -->

**Network:** <!-- e.g. POST /api/auth/login → 422 Unprocessable Entity -->
