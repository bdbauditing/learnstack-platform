name: QA Suite — TaskForge 2.0

# TODO: Fill in the trigger. The workflow should run on push to main and release/* branches.
on:
  push:
    branches:
      - TODO  # replace with: main
      - TODO  # replace with: release/*

jobs:
  playwright-tests:
    name: Playwright Test Suite
    runs-on: ubuntu-latest

    steps:
      # TODO: Step 1 — Check out the repository code
      # Hint: use actions/checkout@v4

      # TODO: Step 2 — Set up Node.js (version 20)
      # Hint: use actions/setup-node@v4 with node-version: '20'

      # TODO: Step 3 — Install npm dependencies
      # Hint: run: npm ci

      # TODO: Step 4 — Install Playwright browsers
      # Hint: run: npx playwright install --with-deps chromium

      # TODO: Step 5 — Run the full Playwright test suite
      # Hint: run: npx playwright test
      # Add: continue-on-error: false  (tests must pass to proceed)

      # TODO: Step 6 — Upload the Playwright HTML report as a workflow artifact
      # Hint: use actions/upload-artifact@v4
      # The artifact name should be 'playwright-report'
      # The path should be 'playwright-report/'
      # Add: if: always()  (upload even if tests fail, so you can see the report)

      # TODO: Step 7 — Notify on failure
      # This step should only run when the tests fail.
      # A simple echo is sufficient:
      #   run: echo "Tests failed — review the Playwright report artifact."
      # Hint: add  if: failure()  to this step
