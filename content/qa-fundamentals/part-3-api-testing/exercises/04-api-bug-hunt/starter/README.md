# Starter — Exercise 04: API Bug Hunt

Test the buggy API at **TODO: actual buggy API URL** using Postman.

Report each bug in `api-bugs.yaml`. You need 3/4 to pass.

Format — use the same bugs.yaml format from Part 1, but for API bugs:
- `location`: the endpoint path (e.g. `POST /tasks`)
- `steps`: API calls instead of UI actions
- `expected` / `actual`: HTTP response status and body
