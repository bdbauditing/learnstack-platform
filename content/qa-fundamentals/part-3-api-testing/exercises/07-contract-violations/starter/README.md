# Starter — Exercise 07: Contract Violation Detection

1. Read `api-spec.yaml` (the official contract)
2. Call each endpoint via Postman
3. Compare actual responses to the spec
4. Report each discrepancy in `violations.yaml`

You need to find all 3 violations to pass.
