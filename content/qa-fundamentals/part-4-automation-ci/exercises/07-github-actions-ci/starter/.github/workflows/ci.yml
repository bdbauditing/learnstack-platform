name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # TODO: add checkout step
      # Hint: uses: actions/checkout@v4

      # TODO: add Node.js setup step
      # Hint: uses: actions/setup-node@v4
      #       with:
      #         node-version: '20'

      # TODO: add npm install step
      # Hint: run: npm ci

      # TODO: add npm test step
      # Hint: run: npm test
