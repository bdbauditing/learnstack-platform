bugs:
  - id: BUG-001
    description: "The checkout flow throws a JavaScript exception on Chrome and no order can be placed by any user."

  - id: BUG-002
    description: "The footer reads 'Copyrigt 2024' instead of 'Copyright 2024'."

  - id: BUG-003
    description: "The bulk data export feature generates empty CSV files. Users cannot export their data."

  - id: BUG-004
    description: "Login works correctly but takes 12 seconds from submit to dashboard. Users are complaining."

  - id: BUG-005
    description: "Canadian users see a dollar sign ($) instead of the Canadian dollar symbol (CA$) in prices."

  - id: BUG-006
    description: "The admin dashboard returns a 403 error for all admin accounts. Admins cannot access admin features."

  - id: BUG-007
    description: "Tooltip text on graph hover is cut off at 30 characters in Internet Explorer 11."

  - id: BUG-008
    description: "The /api/users endpoint returns full user PII including email and phone number without requiring an Authorization header."

  - id: BUG-009
    description: "The search results page shows 11 items when the user requests 10 per page. The extra item is duplicated."

  - id: BUG-010
    description: "Password reset links expire after 5 minutes. The security policy requires them to be valid for 1 hour."
