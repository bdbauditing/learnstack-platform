# Starter — Exercise 03: Negative Testing

Build a collection with 6 negative cases. Each must:
1. Send a deliberately wrong request (bad token, missing field, wrong role, non-existent ID, malformed JSON, wrong password)
2. Assert the correct error status code
3. Assert the error response body is not empty

See the exercise README for the exact 6 cases required.
